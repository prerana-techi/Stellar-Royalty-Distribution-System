{
  "name": "stellar-royalty-distribution-system",
  "version": "1.0.0",
  "private": true,
  "description": "On-Chain Royalty Distribution System on Stellar",
  "main": "wallet.ts",
  "module": "index.ts",
  "types": "wallet.ts",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "test": "npm run test --workspace=frontend"
  },
  "dependencies": {
    "@stellar/freighter-api": "^6.0.1",
    "@stellar/stellar-sdk": "^16.0.1"
  }
}
